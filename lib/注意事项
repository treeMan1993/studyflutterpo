
    在dart语言中，有时候，在等于(=) 两边写空格是要报错的。
    在将String类型转换成Number类型的时候，尽量用double来做，这样不容易出错。
    dart语言的特性就是能简略就简略。
    dart中，$是转译符，如果不想$转译某个字符就用{}花括号包裹起来。这样，就不会被转译
    dart语言不会提示导包，也不会有补全提示。
    dart语言中,访问权限有公有和私有两种，私有，需要在变量，函数，类名前面加_下划线。其余则是私有
    在闭包(一个函数，调用结果是返回一个函数)中，声明的属性是无法被回收的。
    在dart中，实例化对象可以省略关键字new
    在flutter中，布局不用xml文件写，而是直接用代码写。需要设置的属性在控件的构造方法里面给出。控件的构造方法有很多可选参数。这些参数就是给控件设置属性的。
    flutrer写界面重要的一个步骤，就是将多层嵌套的代码分离出来。把嵌套的控件分离出来。在用到的地方再写回去。












